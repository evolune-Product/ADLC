# Vite inlines these at BUILD time, not runtime — rebuild after changing them.
VITE_API_URL=http://localhost:8000
VITE_SOCKET_URL=http://localhost:8000

# Only needed if you use the "Sign in with GitHub" button.
VITE_GITHUB_CLIENT_ID=
